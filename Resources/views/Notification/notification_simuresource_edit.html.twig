{#
path : here, open the resource (route name in controller)
notification : translation file
#}
<a href="{{ path('cpasimusante_simuresource_resource_open', {'resourceId' : notification.details.resource.id}) }}" target="_blank">
    {{ notification.actionKey|trans({
        '%simuresource%' : notification.details.resource.name,
        '%newState%' : notification.details.newState|trans({
        },'resource')
    }, 'notification') }}
</a>